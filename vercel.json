{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install --force",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --force"
      }
    }
  ]
} 