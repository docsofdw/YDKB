import { createClientComponentClient } from \"@supabase/auth-helpers-nextjs\"; const supabase = createClientComponentClient(); // The storePlayerImage and getPlayerImage functions have been removed // as part of the removal of the AI image generation integration
